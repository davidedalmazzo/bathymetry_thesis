# Verified FRF THREDDS/OPeNDAP endpoints are public: no token or password.
FRF_AUTH_MODE=anonymous
# Informational template only; the current client does not read credentials.
# If an endpoint responds 401/403, stop and verify its official authentication
# mechanism before implementing an env/file-backed authenticated adapter.
# Never put credentials, signed URLs or disabled TLS verification here.
